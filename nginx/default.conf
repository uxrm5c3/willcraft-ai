server {
    listen 80;
    server_name will.lifa.com.my;

    # Cloudflare handles SSL termination, so port 80 is fine
    # If you want full (strict) SSL, uncomment the 443 block below

    client_max_body_size 20M;

    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    location /static/ {
        proxy_pass http://web:8000/static/;
        proxy_cache_valid 200 1d;
        expires 1d;
        add_header Cache-Control "public, immutable";
    }
}

# Uncomment for Cloudflare Full (Strict) SSL mode:
# Place Cloudflare Origin Certificate at nginx/certs/cert.pem and nginx/certs/key.pem
#
# server {
#     listen 443 ssl;
#     server_name will.lifa.com.my;
#
#     ssl_certificate /etc/nginx/certs/cert.pem;
#     ssl_certificate_key /etc/nginx/certs/key.pem;
#
#     client_max_body_size 20M;
#
#     location / {
#         proxy_pass http://web:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 120s;
#     }
# }
